<%= form_with model: [commentable, Comment.new] do |form| %>
  <div>
    <%= form.label :body, 'コメント' %>
    <%= form.textarea :body, required: true %>
  </div>
  <div>
    <%= form.submit '投稿する' %>
  </div>
<% end %>
